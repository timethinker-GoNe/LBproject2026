using UnityEngine;

namespace FarmingEngine
{
    /// <summary>
    /// 이 컴포넌트가 붙은 오브젝트 위에서만 GPU 잔디(GpuGrassTool)가 생성된다.
    /// 경작 가능한 땅(흙 Plane) 등에는 붙이지 않는다.
    /// </summary>
    public class GrassGroundZone : MonoBehaviour
    {
    }
}
